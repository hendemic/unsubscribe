[package]
name = "unsubscribe-core"
version = "0.1.2"
edition = "2024"
license = "GPL-3.0-only"
authors = ["Mike Henderson"]

[dependencies]
anyhow = "1"
base64 = "0.22"
encoding_rs = "0.8"
scraper = "0.22"
serde = { version = "1", features = ["derive"] }
url = "2"
